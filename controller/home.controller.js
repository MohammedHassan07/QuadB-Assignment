const home = (req, res) => {


    try {

        // fetch the data from the URL https://api.wazirx.com/api/v2/tickers


        // sotre the data into database

        // send the data to the client

    } catch (error) {
        console.log('homeController -> ', error)
    }
}

module.exports = {

    home
}